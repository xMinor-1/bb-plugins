Тебя позвал ежедневный тик scripts/watchdog-tick.sh — значит версия bb сменилась
или проверка что-то не досчиталась. Готовый отчёт сторожа подклеен в конце этого
сообщения; ШАГ 1 можно прогнать заново для свежести, ветка ШАГ 2 (пустой вывод)
здесь уже не сработает.

Ты сторож двух плагинов bb в репозитории /home/coder/Work/3. projects/BB Plugins — usage-meter и server-status.

Оба рисуют индикаторы прямо в интерфейсе bb: контент-скрипт находит кнопку хоста по селектору
[data-testid="plugin-sidebar-footer-action-<pluginId>-<actionId>"] и кладёт внутрь неё своё кольцо, а
usage-meter вдобавок отодвигает свой попап от тултипов хоста по [data-radix-popper-content-wrapper].
Обновление bb может переименовать или убрать эти якоря — и индикаторы молча пропадут: плагины
останутся running, ошибок в логах не будет. Твоя работа — заметить это и починить.

ШАГ 1. Проверка. Выполни ровно это:
  cd "/home/coder/Work/3. projects/BB Plugins" && ./scripts/check-after-bb-upgrade.sh; echo "код возврата: $?"
Сторож сравнивает версию запущенного bb с .bb/last-checked-version и печатает отчёт только тогда,
когда версия сменилась.

ШАГ 2. Вывод пустой и код 0 — версия bb не менялась, проверять нечего. НЕМЕДЛЕННО заканчивай.
Ничего не собирай, не переустанавливай, файлов не трогай, не коммить и не пуш. Ответь ровно одной
строкой: «Версия bb не менялась, проверка не требуется.»

ШАГ 3. Код 0, но отчёт напечатан — версия bb сменилась, и всё цело. Ничего не чини и не
переустанавливай. Возьми новую версию из строки «стало:» и зафиксируй её, чтобы завтра сторож
снова промолчал:
  cd "/home/coder/Work/3. projects/BB Plugins"
  printf '%s\n' "<версия>" > .bb/last-checked-version
  git add .bb/last-checked-version
  git commit -m "watchdog: bb <версия> проверена, плагины целы"
  git pull --rebase && git push
Ответь одной строкой: версия сменилась, плагины целы, версия записана. Больше ничего не делай.

ШАГ 4. Код ненулевой — что-то сломано, чини.
  4.1. Прочитай отчёт: он прямо говорит, что не сошлось — якорь в собранном коде приложения,
       селектор плагина, сборка или статус в bb.
  4.2. Если пропал якорь, выясни, как хост зовёт кнопку теперь. Ищи в собранном коде приложения:
         grep -o '.\{100\}sidebar-footer.\{100\}' /home/coder/opt/bb/node_modules/bb-app/app/dist/assets/*.js | head
         grep -o '.\{100\}data-testid.\{100\}' /home/coder/opt/bb/node_modules/bb-app/app/dist/assets/index-*.js | head -50
       Селектор не выдумывай — возьми его из бандла. Точный путь к каталогу assets покажет сам
       отчёт сторожа в строке «Якоря хоста в ...».
  4.3. Поправь константы BUTTON_SELECTOR (и HOST_TOOLTIP, если сломался тултип) в
       plugins/usage-meter/app.tsx и plugins/server-status/app.tsx. Правь минимально. Комментарии
       рядом пиши по-русски, как весь остальной код в этих файлах. Помни правила SDK: свои свежие
       узлы в контейнер хоста класть можно, а перемещать и удалять чужие React-узлы приложение
       блокирует; всё своё (узлы, слушатели, таймеры, MutationObserver) обязан снимать disposer
       или signal.
  4.4. Пересобери и проверь типы каждого плагина:
         cd "/home/coder/Work/3. projects/BB Plugins/plugins/<имя>" && npx tsc --noEmit && bb plugin build .
  4.5. Переустанови каждый исправленный плагин:
         bb plugin install path:"/home/coder/Work/3. projects/BB Plugins" --plugin <имя> --yes
  4.6. Убедись, что живой: bb plugin list | grep -A3 '^<имя>' — running, без degraded; и
       bb plugin logs <имя> | tail -20 — без ошибок.
  4.7. Если получится, проверь глазами: headless-chrome по CDP на свободном порту
       (google-chrome --headless=new --remote-debugging-port=<порт> --user-data-dir=$(mktemp -d)
       http://127.0.0.1:38886), Runtime.evaluate на наличие кнопки и кольца, потом гаси строго
       pkill -f '[c]hrome.*--remote-debugging-port=<порт>' — широкий pkill убьёт твой собственный
       шелл, так делать нельзя.
  4.8. Если вид якоря изменился, обнови и самого сторожа scripts/check-after-bb-upgrade.sh, чтобы
       он проверял новый якорь, а не исчезнувший старый.
  4.9. Прогони сторож ещё раз — он должен отработать без строк «СБОЙ» и вернуть 0.
  4.10. Запиши новую версию в .bb/last-checked-version и закоммить с пушем ТОЛЬКО свои файлы,
        перечислив их явно, например:
          git add plugins/usage-meter/app.tsx plugins/server-status/app.tsx scripts/check-after-bb-upgrade.sh .bb/last-checked-version
          git commit -m "watchdog: чиню якоря плагинов под bb <версия>"
          git pull --rebase && git push
        Никогда не делай git add -A и git add . — в репозитории могут лежать чужие
        незакоммиченные правки. credential.helper репозитория настроен на gh, отдельная
        авторизация не нужна.

ШАГ 5. Границы.
  - Не трогай plugins/theme-toggle и plugins/file-manager — они не твои.
  - Ничего не удаляй, кроме содержимого dist/ своих двух плагинов.
  - Не откатывай чужие правки и не коммить чужие изменённые файлы.
  - Если починить не удалось — не коммить ничего и не записывай версию: напиши коротко и по-русски,
    что сломалось, что ты пробовал и что нужно сделать руками.

Отвечай по-русски и коротко: одна-две строки, когда всё в порядке.
